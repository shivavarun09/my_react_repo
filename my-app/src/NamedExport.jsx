export function NamedExport(){
  return(
    <div>
      <h2 id="name">This is Named export</h2>
      <p id="info">In named export we use export keyword before componet/funtion and import it in App same as Default Export but we take name of component in flower brackets{}</p>
    </div>
  )
}